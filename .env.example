# Required: Secret for cron endpoint authentication
# Generate with: openssl rand -hex 32
CRON_SECRET=

# Optional: Secret for API endpoint authentication
# Generate with: openssl rand -hex 32
API_SECRET=

# Optional: Encryption key for storing API tokens (64 hex chars = 32 bytes)
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=

# Vercel KV is auto-configured when using the Deploy button
# These are set automatically:
# KV_URL=
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
# KV_REST_API_READ_ONLY_TOKEN=
